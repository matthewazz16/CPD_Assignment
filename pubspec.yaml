name: flutter_application_1
description: A new Flutter project

publish_to: 'none' # Remove this line if you're publishing the app

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^0.8.4+4  # Add the image_picker dependency here
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
